userspace.party {
    encode zstd gzip

    root * /srv/userspace-party/current
    try_files {path}/index.html {path} /index.html

    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        Referrer-Policy "strict-origin-when-cross-origin"
    }

    file_server
}
